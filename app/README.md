# app/

Future backend/API server — auth, progress tracking (lessons completed, streaks, mastery), and eventually serving curriculum/lesson data over an API instead of bundling it directly into the frontend.

Not started. `frontend/` currently reads all data locally; this becomes necessary once progress needs to persist across sessions or devices.
